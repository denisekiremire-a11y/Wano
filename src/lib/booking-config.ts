// Instant-mode booking timing — kept here, not scattered through the
// code, same reasoning as xp-config.ts's pricing/capacity constants.
export const HOLD_MINUTES = 10;
export const REQUEST_EXPIRY_HOURS = 2;
export const CANCELLATION_CUTOFF_HOURS = 24;
export const SIMILAR_LISTINGS_LIMIT = 3;
